bisim ? a?||b? ~ a?,b? + b?,a? ;
fbisim ? a?||b? ~ a?,b? + b?,a? ;

def Switch = Switch(off);
def Switch(off) = on?,Switch(on);
def Switch(on) = off?,Switch(off);

def Switch2 = on?,off?,Switch2;

bisim ? Switch ~ Switch2;
fbisim ? Switch ~ Switch2;

bisim ? a?,(b? + c?) ~ a?,b? + a?,c? ;
fbisim ? a?,(b? + c?) ~ a?,b? + a?,c? ;


